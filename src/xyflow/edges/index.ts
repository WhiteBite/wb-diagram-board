import { ArrowEdge } from './ArrowEdge';
import { ConnectorEdge } from './ConnectorEdge';

export const edgeTypes = {
    arrow: ArrowEdge,
    connector: ConnectorEdge,
};

export { ArrowEdge, ConnectorEdge };
